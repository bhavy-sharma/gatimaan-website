<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= course.title %> - Course Details</title>
  <script src="https://cdn.tailwindcss.com"></script> 
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
    }
  </style>
</head>
<body class="bg-white text-gray-800">

  <!-- Header -->
  <header class="border-b border-gray-200 py-6 px-4 sm:px-6 lg:px-8">
    <div class="max-w-6xl mx-auto">
      <h1 class="text-3xl font-bold text-gray-900"><%= course.title %></h1>
      <p class="text-sm text-gray-500 mt-1">Explore more and start learning today</p>
    </div>
  </header>

  <!-- Main Content -->
  <main class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-10 grid grid-cols-1 lg:grid-cols-3 gap-8">

    <!-- Left Column - Text Content -->
    <div class="lg:col-span-2 space-y-6">

      <!-- Description -->
      <section class="bg-gray-50 p-6 rounded-lg shadow-sm border border-gray-200">
        <h2 class="text-xl font-semibold text-gray-800 mb-3">Description</h2>
        <p class="text-gray-600 leading-relaxed"><%= course.shortDescription %></p>
      </section>

      <!-- Long Description -->
      <section class="bg-gray-50 p-6 rounded-lg shadow-sm border border-gray-200">
        <h2 class="text-xl font-semibold text-gray-800 mb-3">What You'll Learn</h2>
        <p class="text-gray-600 leading-relaxed"><%= course.longDescription %></p>
      </section>

      <!-- Admission Process -->
      <section class="bg-gray-50 p-6 rounded-lg shadow-sm border border-gray-200">
        <h2 class="text-xl font-semibold text-gray-800 mb-3">Admission Process</h2>
        <p class="text-gray-600 leading-relaxed">Yaha addmission process ayaga</p>
      </section>

    </div>

    <!-- Right Column - Image -->
    <div class="flex justify-center">
      <div class="w-full max-w-md overflow-hidden rounded-lg shadow-md border border-gray-200 transition-transform duration-300 hover:scale-105">
        <img 
          src="<%= course.imageUrl || 'https://placehold.co/600x400' %>" 
          alt="<%= course.title %>" 
          class="w-full h-auto object-cover"
        >
      </div>
    </div>

  </main>

  <!-- WhatsApp Button -->
   <%- include("./components/whatsappIcon.ejs") %>

</body>
</html>