<header class="navbar">
  <div class="logo">Gatimaan</div>

  <button class="hamburger" id="hamburger">
    ☰
  </button>

  <nav class="nav-links" id="nav-links">
    <button class="nav-btn"><a href="/">Home</a></button>
    <button class="nav-btn"><a href="/layout/courses" class="highlight">Courses</a></button>
    <button class="nav-btn"><a href="/layout/about">About</a></button>
    <button class="nav-btn"><a href="/layout/contact">Contact</a></button>
    <button class="nav-btn"><a href="/news" class="highlight">News & Event</a></button>
  </nav>

  <div class="contact-button">
    Call us : +91 0000000000 <br>
    Email us : account@gatimaan.com
  </div>
</header>

<script>
const hamburger = document.getElementById('hamburger');
const navLinks = document.getElementById('nav-links');

// Toggle menu on hamburger click
hamburger.addEventListener('click', () => {
  navLinks.classList.toggle('active');
});

// Optional: Close menu when clicking outside
document.addEventListener('click', function(event) {
  const isClickInsideNav = navLinks.contains(event.target);
  const isClickOnHamburger = hamburger.contains(event.target);

  if (!isClickInsideNav && !isClickOnHamburger && navLinks.classList.contains('active')) {
    navLinks.classList.remove('active');
  }
});
</script>

